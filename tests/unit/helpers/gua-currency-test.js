
import { guaCurrency } from 'donation/helpers/gua-currency';
import { module, test } from 'qunit';

module('Unit | Helper | gua currency');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = guaCurrency([42]);
  assert.ok(result);
});

